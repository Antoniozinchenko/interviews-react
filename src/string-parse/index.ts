/**
 * 
 * This method parses input object and retrieve all strings which are inside the input
 * 
 * @param input - can be any type (except function) which contains some fields or elements 
 * @returns {string[]} array of all founded strings
 */
export function findStrings(input: any) : string[] {
  console.log('input object:', input);
  const result: string[] = []; 
  return result;
}